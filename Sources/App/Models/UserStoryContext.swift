// TODO: I'm questioning if we need "SomethingContext" struct
// it could rather be part of UserStory extension or something like that.
// We have no usage now to provide the Context.
struct UserStoryContext {
    static let maximumAllowed = 20
}
